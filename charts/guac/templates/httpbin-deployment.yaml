# Copyright Kusari, Inc. and contributors
# Licensed under the MIT license. See LICENSE file in the project root for details.
{{ if .Values.guac.httpbin.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.guac.httpbin.name }}
{{- if .Values.guac.httpbin.annotations }}
  annotations:
{{ toYaml .Values.guac.httpbin.annotations | indent 4 }}
{{- end }}
  labels:
    {{- include "guac.labels" . | nindent 4 }}
    app.kubernetes.io/name: {{ .Values.guac.httpbin.name }}
    app.kubernetes.io/component: {{ .Values.guac.httpbin.name }}
spec:
  replicas:  {{ .Values.guac.httpbin.replicas }}
  selector:
    matchLabels:
      {{- include "guac.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/name: {{ .Values.guac.httpbin.name }}
      app.kubernetes.io/component: {{ .Values.guac.httpbin.name }}
  template:
    metadata:
      labels:
        {{- include "guac.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/name: {{ .Values.guac.httpbin.name }}
        app.kubernetes.io/component: {{ .Values.guac.httpbin.name }}
    spec:
      containers:
      - name: {{ .Values.guac.httpbin.name }}
        {{- if .Values.guac.httpbin.image.digest }}
        image: "{{ .Values.guac.httpbin.image.repository }}@{{ .Values.guac.httpbin.image.digest }}"
        {{- else }}
        image: "{{ .Values.guac.httpbin.image.repository }}:{{ .Values.guac.httpbin.image.tag | default .Chart.AppVersion}}"
        {{- end }}
        imagePullPolicy: "{{ .Values.guac.httpbin.image.pullPolicy }}"
{{- if .Values.guac.httpbin.image.command }}
        command:
{{ toYaml .Values.guac.httpbin.image.command | indent 10 }}
{{- end }}
{{- if .Values.guac.httpbin.image.ports }}
        ports:
{{ toYaml .Values.guac.httpbin.image.ports | indent 10 }}
{{- end }}
        volumeMounts:
          - name: guac-config
            mountPath: /workspace/guac/guac.yaml
            subPath: guac.yaml
            readOnly: true
{{- if .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.imagePullSecrets | indent 8 }}
{{- end }}
      volumes:
        - name: guac-config
          configMap:
            name: guac-cm
{{- end }}